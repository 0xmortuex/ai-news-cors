{
  "name": "ai-news-cors",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker: CORS passthrough proxy + Trendshift trending-repo scraper for ai-news-tracker.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "node --test test/",
    "test:personalize": "node --test test/personalize.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.92.0"
  }
}
